#include <iostream>
#include "List.h"
#include "Stack.h"

using namespace std;

int main() {
    /*List list;

    list.insertFirst(1);
    list.insertFirst(2);
    list.insertFirst(3);
    list.print();

    while(!list.empty()) {
        cout<< list.removeFirst() << " element is removed" << endl;
    }

    cout<<"The empty list is: "<<endl;
    list.print();

    for (int i = 0; i < 10; ++i) {
        list.insertFirst(i);
    }

    cout<<"The new list: "<<endl;
    list.print();

    int num = 0;
    cout<<"Enter a number which you want to verify: ";
    cin>>num;

    if(list.exists(num)) {
        cout<<"The number "<<num<<" exists in the list"<<endl;
    } else {
        cout<<"The number "<<num<<" does not exist in the list"<<endl;
    }

    cout<<"The size of the list is: "<<list.size()<<endl;

    list.remove(5, List::DeleteFlag::EQUAL);

    cout<<"After the delete operation: "<<endl;
    list.print();*/

    /*Stack stack(10);

    stack.push(10);
    stack.push(12);
    stack.push(14);

    stack.pop();
    stack.pop();
    stack.pop();

    if(stack.isEmpty()) {
        cout<<"The stack is empty"<<endl;
    } else {
        cout<<"The stack is not empty"<<endl;
    }*/

    return 0;
}
